#include "minishell.h"

int	ft_char(char *str)
{
	while (str)
	{
		if (ft_isalnum(c))
	if (ft_isspace(c))
		return (space);
	/* 	tokenword
		tokenspace
		tokentiret
		token'
		token"
		token&&
		token|
		token||
		token()
		token$
		token<
		token<<
		token>
		token>>
		 */
	}
}

void	ft_tokencreate(t_minishell *minishell, char *str)
{
	ft_char(str);
}